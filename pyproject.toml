[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "did-testing-mpt-core"
version = "0.1.0"
description = "Standalone application-level Merkle Patricia Trie for testing traceability"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["mpt_core*"]
